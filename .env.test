# Test environment settings for GhostWriter
# This file is automatically loaded by pytest via tests/conftest.py
# It should keep tests deterministic and independent of your personal .env

# Use a sandbox location for the active test book base
GW_BOOK_BASE_DIR=sandbox/LittleRedRidingHood

# You can optionally pin other GW_* settings for tests here
# (leave unset to exercise defaults in code)
